---
title: "Delete Account"
api: "DELETE https://api.return.com/api/account"
description: "This endpoint deletes an existing account."
---

### Body

<ParamField body="id" type="string">
  The ID of the account to delete.
</ParamField>


### Response

<ResponseField name="success" type="number">
  Indicates whether the call was successful. 1 if successful, 0 if not.
</ResponseField>

<RequestExample>

```bash cURL
curl --location --request DELETE 'https://api.stateset.com/api/account' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token <token>' \
--data-raw '{
		"id": ""
}'
```

```graphQL GraphQL

mutation {
  delete_accounts(
    where: { id: { _eq: "" } }
  ) {
    affected_rows
  }
}

```

```javascript Node.js

var deleted = await stateset.accounts.del({
  "1"
});

```

</RequestExample>

<ResponseExample>

```json Response
{
  {
    "data": {
        "delete_accounts": {
            "affected_rows": 1
        }
    }
}
}
```

</ResponseExample>
